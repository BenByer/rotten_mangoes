

  <%= user.id %> - User: 
  <%= puts "User:  #{user.id}" %>
  <strong><%= link_to user.full_name, edit_admin_user_path(user) %></strong> | 
  <%= puts "Email:  #{user.email}" %>
  Email: <%= user.email %>
  <br>
  Admin: <%= user.is_admin %>
  <br>
  <%= link_to "Delete", admin_user_path(user), method: :delete, data: {confirm: "Are you sure you want to delete this user?"} %> | <%= link_to "Edit", edit_admin_user_path(user) %>
<hr>